# Main Docker Compose file
# Langfuse: managed via Langfuse Cloud (https://cloud.langfuse.com)
# ClickHouse MCP: managed via ClickHouse Cloud (https://mcp.clickhouse.cloud/mcp)

include:
  - librechat-compose.yml
